spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fpc_example
    username: example
    password: 8MEfb!n>E-(y&]r;

  jpa:
    show-sql: false
    hibernate.ddl-auto: create
    hibernate.dialect.storage_engine: innodb
    properties:
      hibernate.format_sql: true
      hibernate.dialect: org.hibernate.dialect.MySQL57Dialect

logging.level:
  web: DEBUG
  org.springframework: INFO
  com.web: DEBUG

server.port: 8080
